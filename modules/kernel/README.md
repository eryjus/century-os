# CenturyOS Kernel Module

In this folder is the kernel module.  It includes the multiboot entry point, the loader for 1-time initialization, and the kernel proper.

Since this is a micro-kernel, the following features are included in the kernel:
* Scheduling
* Messaging
* Physical and Virtual memory management


